Create a plan to build a port of solar_os to the zeroink writer.

Use technical information from the ize-compose repo which is a 3rd party open source firmware for the zeroink writer. And the zerowriter_ink repo which contains CAD files and the binary firmware of the not-yet open source official firmware.
